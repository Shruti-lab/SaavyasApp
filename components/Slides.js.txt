import {View,Text,Dimensions,StyleSheet} from 'react-native';
import {SliderBox} from 'react-native-image-slider-box';
const images = [
    require('../assets/slide1.jpg'),
    require('../assets/slide2.jpg'),
    require('../assets/slide3.jpg')

]
const WIDTH = Dimensions.get('window').width;
const HEIGHT = Dimensions.get('window').height;

export default function Slides(){
    
    return(
        <View style={slide.container}>
            <View>
            <SliderBox style={slide.wrap} images={images} dotColor='blue' inactiveDotColor='white' dotStyle={{
                height:20,width:20,borderRadius:50}} 
                imageLoadingColor="#111e22"
                circleLoop={true}
                paginationBoxVerticalPadding={20}

                />
                </View>
        </View>
    )
}

const slide = StyleSheet.create({
    container:{
        justifyContent:'center',
        alignContent:'center'
    },
    wrap:{
        width:WIDTH*0.75,
        height:HEIGHT*0.25
    }
})